package com.bank.bian.tradeordermanagement;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.annotation.Bean;

/**
 * BIAN Service Domain: Trade Order Management
 *
 * Business Area:      Operations and Execution
 * Business Domain:    Securities
 * Functional Pattern: Manage
 * Asset Type:         Trade Order
 * Control Record:     Trade Order Management Plan
 *
 * Generated by the bian-platform service generator (Phase 1 — shallow build).
 * Replace the in-memory store with real persistence and domain logic in Phase 2.
 */
@SpringBootApplication
public class Application {

    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }

    @Bean
    public OpenAPI openApi() {
        return new OpenAPI().info(new Info()
                .title("Trade Order Management — BIAN Semantic API")
                .description("Service Domain 'Trade Order Management' (Manage pattern) "
                        + "operating on the 'Trade Order Management Plan' control record.")
                .version("0.1.0"));
    }
}
